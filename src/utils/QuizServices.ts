import { Quiz } from '@/interfaces/Quiz';

class QuizServices {
  answers: string[] = [];

  // 퀴즈 불러오기
  // 퀴즈 랜덤 로직
  // 퀴즈 타입에 따른 보기 생성하기
  // 유저가 쓴 답과 정답 비교하기
  // 유저가 얻을 점수 계산하기 -> difficulty, importance
}

export default new QuizServices();
